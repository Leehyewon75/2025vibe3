import streamlit as st
import pandas as pd

uploaded_file = st.file_uploader("CSV 파일 업로드", type=["csv"])
if uploaded_file:
    df = pd.read_csv(uploaded_file, encoding='euc-kr')  # 인코딩 필요하면 조정
    st.write("컬럼명:", df.columns.tolist())  # 컬럼명 리스트 출력
    st.write(df.head())  # 데이터 샘플 확인
